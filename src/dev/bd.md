# Базы данных

Данные, добытые из сети, забирать мы научились. Теперь вопрос, где их держать. Файлов хватает до тех пор, пока накопленное не понадобилось нескольким людям одновременно.

## Когда файлов перестаёт хватать

Эволюция хранения данных в лаборатории почти всегда идёт по одному сценарию, знакомому каждому. Сначала появляется `result.csv`. Потом `result_final.csv`, `result_final_v2.csv` и папка `2026-08-20/` с двадцатью файлами, названными по энергии пучка, номеру захода и фамилии оператора. Через год у тебя десятки гигабайт CSV-файлов, и вопрос «покажи все измерения канала BPM01 при энергии выше 10 МэВ» превращается в вечер со скриптом, рекурсивно обходящим папки и разбирающим имена файлов.

Файлы и папки ломаются на трёх вещах, перечисленных ниже:

- **Конкурентный доступ.** Скрипт сбора данных пишет в файл, а ты в этот момент открываешь его для анализа и читаешь недописанную строку. Два процесса пишут одновременно, и файл оказывается повреждённым. У файловой системы нет понятия «одновременно и безопасно».
- **Целостность.** CSV не проверяет ничего. В столбец с температурой попадает строка `"error"`, в одном файле столбцы идут в одном порядке, в другом файле в другом, а измерение ссылается на заход, который кто-то удалил вместе с папкой.
- **Поиск.** Чтобы найти нужные записи, приходится читать *все* файлы целиком в память. На миллионах записей это минуты; при данных, разложенных правильно, тот же запрос выполняется за миллисекунды.

Все три задачи решает **СУБД** — система управления базами данных. Она берёт на себя хранение, разводит одновременный доступ, следит за целостностью и даёт язык, приспособленный для запросов, чтобы ты описывал, *что* найти, а не *как* перебирать файлы.

## Реляционная модель

Реляционную модель, предложенную Эдгаром Коддом в 1970 году, до сих пор считают основной. Идея простая. Данные укладываются в **таблицы**, называемые в теории отношениями (relations). Строка таблицы содержит запись об одном объекте, а столбец задаёт атрибут, наделённый фиксированным типом.

Спроектируем схему для эксперимента, устроенного типично. У тебя есть измерительные кампании, внутри каждой лежат заходы (runs), различающиеся параметрами установки, а внутри каждого захода точки измерений, снятые с датчиков.

```
experiments ──< runs ──< measurements
   (1)          (N)          (N)
```

- `experiments` описывает кампанию, её название, установку и дату начала;
- `runs` описывает заход, его кампанию, номер, энергию пучка и оператора;
- `measurements` описывает отдельную точку, её заход, канал, время и значение.

Знак `──<` читается как связь «один ко многим»: у одной кампании много заходов, а у одного захода много снятых точек.

### Ключи и связи

**Первичным ключом** (primary key) называют столбец или набор столбцов, однозначно идентифицирующий строку. Обычно это суррогатный целочисленный `id`, выдаваемый базой самостоятельно.

**Внешним ключом** (foreign key) называют столбец, ссылающийся на первичный ключ другой таблицы. `runs.experiment_id` хранит `id` кампании, и так строки связываются между собой. СУБД следит за валидностью проставленной ссылки. Нельзя вставить заход с `experiment_id = 42`, если кампании с таким `id` нет, и нельзя удалить кампанию, на которую ссылаются заходы.

Связь «многие ко многим» (например, публикации и авторы) выражается через промежуточную таблицу, снабжённую двумя внешними ключами, и отдельного механизма для неё не нужно.

### Нормализация

Соблазнительно свалить всё в одну широкую таблицу, повторяя в каждой строке измерения название кампании, энергию и фамилию оператора. Работает схема, устроенная так, до первой правки. Оператор оказался «Ивановым», а не «Ивановым А.», и обновлять надо миллион строк; а если скрипт упадёт на середине, данные останутся противоречивыми. Ситуации, возникающие так, называются аномалиями обновления.

**Нормализацией** называют устранение дублирования, при котором каждый факт хранится один раз, а всё остальное на него ссылается. Формально она описывается нормальными формами, а для практики достаточно первых трёх, сводящихся к мнемонике: неключевые атрибуты зависят «от ключа, от всего ключа и ни от чего, кроме ключа». Энергия остаётся свойством захода, а не точки измерения, значит, ей место в `runs`. Название установки принадлежит кампании, значит, лежит в `experiments`.

Обратный процесс — денормализация — тоже бывает осмысленным. Для тяжёлой аналитики иногда специально собирают широкую таблицу, чтобы не соединять разложенные данные на каждый запрос. Но начинают всегда с нормализованной схемы.

## Основы SQL на примере SQLite

**SQL** (Structured Query Language) представляет собой декларативный язык, приспособленный для работы с реляционными базами. Ты описываешь нужный результат, а СУБД сама решает, как его получить.

Учиться будем на **SQLite**. Это встраиваемая СУБД, работающая без всякого сервера: вся база у неё лежит в одном файле, положенном на диск, а драйвер входит в стандартную библиотеку Python (модуль `sqlite3`). При этом SQLite остаётся полноценной реляционной СУБД с транзакциями, спокойно работающей с базами в десятки гигабайт. Для лабораторных данных этого более чем достаточно.

Открыть консоль SQLite можно командой `sqlite3 lab.db`, создающей файл сама, а посмотреть на созданную базу глазами позволяет программа [DB Browser for SQLite](https://sqlitebrowser.org/).

### CREATE TABLE: создаём таблицы

Схема из трёх таблиц, нарисованная выше, на SQL выглядит почти так же, как читалась словами. Приписки, идущие после типов, задают ограничения, соблюдаемые базой самостоятельно, что бы ни делал твой код.

```sql
CREATE TABLE experiments (
    id         INTEGER PRIMARY KEY,   -- первичный ключ, база выдаёт сама
    name       TEXT NOT NULL UNIQUE,  -- название кампании, без повторов
    setup      TEXT,                  -- описание установки
    started_at TEXT NOT NULL          -- дата в ISO-формате: '2026-08-20'
);

CREATE TABLE runs (
    id            INTEGER PRIMARY KEY,
    experiment_id INTEGER NOT NULL REFERENCES experiments(id),  -- внешний ключ
    run_number    INTEGER NOT NULL,
    energy_mev    REAL NOT NULL,      -- энергия пучка в заходе, МэВ
    operator      TEXT,
    started_at    TEXT NOT NULL,
    UNIQUE (experiment_id, run_number)  -- номер захода уникален внутри кампании
);

CREATE TABLE measurements (
    id      INTEGER PRIMARY KEY,
    run_id  INTEGER NOT NULL REFERENCES runs(id),
    channel TEXT NOT NULL,            -- имя канала: 'BPM01', 'T_magnet', ...
    t       REAL NOT NULL,            -- время от начала захода, с
    value   REAL NOT NULL             -- измеренное значение
);
```

Ограничения `NOT NULL`, `UNIQUE` и внешние ключи и дают ту целостность, которой не было у CSV. Попытка записать мусор закончится ошибкой, а не порчей, которая всплывёт через полгода.

У SQLite здесь есть подвох, на который натыкаются все. Внешние ключи она проверяет только при проверке, включённой явно, и включать её надо в каждом новом соединении:

```sql
PRAGMA foreign_keys = ON;
```

Без этой строки вставка ссылки на несуществующую кампанию пройдёт молча, и база тихо наберёт битых связей, которые всплывут при первом же `JOIN`. PostgreSQL и MySQL проверяют внешние ключи всегда, без всяких оговорок.

### INSERT: вставляем данные

Вставка добавляет строки. Заполняемые базой столбцы перечислять не нужно, `id` она выдаст очередной. А вот `experiment_id` для захода придётся указать, иначе непонятно, к какой кампании он относится.

```sql
INSERT INTO experiments (name, setup, started_at)
VALUES ('Калибровка датчиков положения', 'стенд ЛИУ', '2026-08-20');

INSERT INTO runs (experiment_id, run_number, energy_mev, operator, started_at)
VALUES (1, 1, 12.5, 'Иванов', '2026-08-20T10:15:00');

-- несколько строк одним запросом
INSERT INTO measurements (run_id, channel, t, value) VALUES
    (1, 'BPM01',    0.0, 0.412),
    (1, 'BPM01',    0.1, 0.418),
    (1, 'T_magnet', 0.0, 42.3);
```

Последняя форма, со списком кортежей, перечисленных после `VALUES`, вставляет пачку строк одним запросом. Для тысячи точек с АЦП это на порядки быстрее тысячи отдельных `INSERT`; причина станет понятна в разделе про транзакции.

### SELECT: выбираем

Ради этого всё и затевалось. Вопрос, требовавший раньше вечера со скриптом-обходчиком папок, теперь пишется в три строки:

```sql
-- все точки канала BPM01 из первого захода, по возрастанию времени
SELECT t, value
FROM measurements
WHERE run_id = 1 AND channel = 'BPM01'
ORDER BY t;

-- десять последних заходов с энергией выше 10 МэВ
SELECT run_number, energy_mev, started_at
FROM runs
WHERE energy_mev > 10
ORDER BY started_at DESC
LIMIT 10;
```

`WHERE` фильтрует строки, `ORDER BY` сортирует отобранное (`DESC` задаёт убывание), `LIMIT` обрезает результат. Звёздочка `SELECT *` вернёт все столбцы, что удобно в консоли, но в написанных скриптах лучше перечислять нужные явно.

### JOIN: соединяем таблицы

Нормализация разложила данные по трём таблицам, а `JOIN` собирает разложенное обратно. Соединение сопоставляет строки по условию, обычно «внешний ключ равен первичному»:

```sql
-- каждая точка вместе с номером захода и названием кампании
SELECT e.name, r.run_number, m.t, m.value
FROM measurements AS m
JOIN runs        AS r ON r.id = m.run_id
JOIN experiments AS e ON e.id = r.experiment_id
WHERE m.channel = 'BPM01'
ORDER BY e.name, r.run_number, m.t;
```

`AS m` задаёт псевдоним, избавляющий от длинных имён. Кроме обычного (внутреннего) `JOIN` есть `LEFT JOIN`, сохраняющий строки левой таблицы даже при отсутствии пары, ожидаемой справа. Так можно, например, найти заходы, оставшиеся вообще без измерений.

### GROUP BY: агрегируем

Агрегатные функции (`COUNT`, `AVG`, `SUM`, `MIN`, `MAX`) сворачивают отобранную группу строк в одно число, а `GROUP BY` задаёт, по чему группировать:

```sql
-- сводка по каждому заходу: число точек и статистика сигнала
SELECT r.run_number,
       r.energy_mev,
       COUNT(*)     AS n_points,
       AVG(m.value) AS mean_value,
       MIN(m.value) AS min_value,
       MAX(m.value) AS max_value
FROM measurements AS m
JOIN runs AS r ON r.id = m.run_id
WHERE m.channel = 'BPM01'
GROUP BY r.id
HAVING COUNT(*) >= 100   -- отбрасываем слишком короткие заходы
ORDER BY r.energy_mev;
```

Не спутай `HAVING` с `WHERE`. Первый фильтрует *после* группировки, по посчитанным агрегатам; второй отбирает строки *до* неё.

### Индексы: ускоряем поиск

Без индекса запрос с `WHERE` заставляет базу просмотреть всю таблицу, строку за строкой. **Индекс** — это дополнительная структура (обычно B-дерево), позволяющая находить строки по значению столбца за логарифмическое время, наподобие предметного указателя, приложенного к книге.

```sql
CREATE INDEX idx_measurements_run_channel
ON measurements (run_id, channel);
```

Теперь выборка точек конкретного канала конкретного захода не зависит от общего размера таблицы. Первичный ключ и `UNIQUE` индексируются автоматически, а вот внешние ключи и столбцы, часто участвующие в фильтрации, приходится индексировать самому. Платой за созданный индекс становятся место на диске и чуть более медленная вставка, поэтому создавать индексы «на всякий случай» на все столбцы не нужно. Проверить, пользуется ли запрос построенным индексом, можно, приписав перед ним `EXPLAIN QUERY PLAN`.

## Тот же SQL из Python

Модуль `sqlite3`, входящий в стандартную библиотеку, устанавливать не требуется.

```python
import sqlite3

# открываем (или создаём) файл базы; ":memory:" — временная база в памяти
con = sqlite3.connect("lab.db")

# в SQLite проверка внешних ключей по умолчанию выключена — включаем
con.execute("PRAGMA foreign_keys = ON")

cur = con.cursor()  # курсор выполняет запросы и отдаёт результаты

# создаём таблицу, если её ещё нет (текст запроса — тот же SQL)
cur.execute("""
    CREATE TABLE IF NOT EXISTS measurements (
        id      INTEGER PRIMARY KEY,
        run_id  INTEGER NOT NULL,
        channel TEXT NOT NULL,
        t       REAL NOT NULL,
        value   REAL NOT NULL
    )
""")

# одиночная вставка: значения передаём кортежем, в запросе — знаки вопроса
cur.execute(
    "INSERT INTO measurements (run_id, channel, t, value) VALUES (?, ?, ?, ?)",
    (1, "BPM01", 0.0, 0.412),
)

# массовая вставка: executemany принимает любой итерируемый объект;
# read_adc() здесь — твоя функция опроса АЦП
points = [(1, "BPM01", 0.1 * i, read_adc()) for i in range(1000)]
cur.executemany(
    "INSERT INTO measurements (run_id, channel, t, value) VALUES (?, ?, ?, ?)",
    points,
)
con.commit()  # фиксируем изменения на диске

# выборка: по курсору можно итерироваться, каждая строка — кортеж
for t, value in cur.execute(
    "SELECT t, value FROM measurements WHERE channel = ? ORDER BY t",
    ("BPM01",),
):
    print(t, value)

con.close()
```

Для небольших выборок удобны `cur.fetchone()` и `cur.fetchall()`, но итерация по курсору экономнее: строки, читаемые по мере надобности, не оседают в памяти все сразу.

### Параметризованные запросы и SQL-инъекции

В примерах, приведённых выше, значения подставлялись через знаки вопроса, а не f-строками. Это не вкусовщина, а безопасность. Представь, что имя канала приходит извне: из формы на веб-странице, из аргумента командной строки или из присланного кем-то файла.

```python
channel = input("Канал: ")

# ТАК ДЕЛАТЬ НЕЛЬЗЯ: запрос склеивается из чужого текста
cur.execute(f"SELECT t, value FROM measurements WHERE channel = '{channel}'")
```

Если пользователь введёт `' OR '1'='1`, итоговый запрос превратится в `... WHERE channel = '' OR '1'='1'`, где условие всегда истинно, наложенный фильтр исчез, а наружу утекла вся таблица. А ввод вида `'; DROP TABLE measurements; --` в СУБД, чей драйвер исполняет несколько команд подряд, просто уничтожит указанную таблицу. Всё это называется **SQL-инъекцией**, одной из самых старых и до сих пор самых распространённых уязвимостей (классику жанра описывает [xkcd про школьника Bobby Tables](https://xkcd.com/327/)).

Параметризованный запрос неуязвим по построению: текст запроса и передаваемые данные уходят драйверу *раздельно*, и введённая строка останется просто строкой, что бы в ней ни было:

```python
# правильно: запрос отдельно, данные отдельно
cur.execute("SELECT t, value FROM measurements WHERE channel = ?", (channel,))
```

Правило простое: **значения передаются только через параметры, всегда**, даже если «данные точно свои». Единственное ограничение, накладываемое на это правило, состоит в том, что параметром может быть лишь значение, но не имя таблицы или столбца. Имена, подставляемые динамически, проверяй по белому списку.

## Транзакции и ACID

**Транзакцией** называют группу запросов, выполняемую как единое целое, поэтому применяются либо все, либо ни один. Гарантии, даваемые транзакцией, классически описываются аббревиатурой **ACID**:

- **Atomicity** (атомарность) означает неделимость транзакции: сбой, случившийся на середине, откатывает всё сделанное;
- **Consistency** (согласованность) требует, чтобы база переходила из одного корректного состояния в другое, не нарушая ограничений (ключи, `NOT NULL`, `UNIQUE`);
- **Isolation** (изолированность) прячет промежуточные состояния, возникающие в параллельных транзакциях, друг от друга;
- **Durability** (долговечность) сохраняет подтверждённые (`COMMIT`) данные даже при внезапно пропадающем питании.

Заход и снятые в нём точки должны попасть в базу вместе: заход без точек и точки-сироты одинаково бессмысленны.

```python
def save_run(con, run_row, points):
    """Сохраняет заход и все его измерения одной транзакцией."""
    cur = con.cursor()
    try:
        # первый INSERT неявно открывает транзакцию
        cur.execute(
            "INSERT INTO runs (experiment_id, run_number, energy_mev, started_at) "
            "VALUES (?, ?, ?, ?)",
            run_row,
        )
        run_id = cur.lastrowid  # id только что вставленной строки
        cur.executemany(
            "INSERT INTO measurements (run_id, channel, t, value) "
            "VALUES (?, ?, ?, ?)",
            [(run_id, ch, t, v) for ch, t, v in points],
        )
        con.commit()    # всё получилось — фиксируем
    except sqlite3.Error:
        con.rollback()  # любая ошибка — откатываем целиком
        raise
```

Ту же пару commit/rollback даёт конструкция `with con:`, подтверждающая транзакцию при выходе из блока и откатывающая её при вылетевшем исключении. Заодно транзакции ускоряют массовую вставку. Миллион `INSERT` с commit после каждого означает миллион синхронизаций с диском, а те же вставки, собранные в одну транзакцию, быстрее на порядки.

## SQLite, PostgreSQL или MySQL

Рано или поздно возникает вопрос, не пора ли перерасти один файл. Три реляционные СУБД, которые встречаются в лаборатории чаще всего, различаются не столько языком запросов, поскольку SQL везде почти один и тот же, сколько тем, кто и как до базы дотягивается.

| | SQLite | PostgreSQL | MySQL / MariaDB |
|---|---|---|---|
| Архитектура | встраиваемая библиотека, вся база в одном файле | клиент-серверная | клиент-серверная |
| Установка | не нужна, есть в Python | отдельный сервис | отдельный сервис |
| Одновременная запись | один пишущий процесс | много клиентов | много клиентов |
| Типизация | динамическая, мягкая | строгая, богатая (массивы, JSONB, диапазоны) | строгая, скромнее |
| Права доступа | нет (права на файл) | пользователи, роли, гранулярные права | пользователи и права |
| Сильная сторона | нулевая настройка, переносимость | функциональность и расширения (PostGIS, TimescaleDB) | распространённость в веб-хостинге |
| Типичный случай | локальные данные, прототипы, файлы до единиц ГБ | серверное приложение, общая база группы | веб-проекты, легаси-системы |

Правило, проверенное практикой, простое. Если данные лежат на твоём диске и работаешь с ними ты и твои скрипты, бери **SQLite** и не выдумывай лишнего. Если база нужна нескольким людям или сервисам по сети, если пишут в неё одновременно и требуются права доступа, бери **PostgreSQL**, сегодня это выбор по умолчанию для серверной СУБД. **MySQL** чаще достаётся как данность вместе с унаследованным проектом или хостингом, чем выбирается осознанно для новой системы.

## ORM: SQLAlchemy

Писать SQL строками внутри Python-кода не всегда удобно. Строки не проверяются до запуска, результаты приходят безликими кортежами, а логика, связывающая объект с его окружением, размазывается по запросам. **ORM** (Object-Relational Mapping) отображает таблицы на классы, строки на объекты, а проставленные внешние ключи на атрибуты-связи. Стандартом де-факто в Python служит [SQLAlchemy](https://docs.sqlalchemy.org/).

```python
from sqlalchemy import ForeignKey, create_engine, select
from sqlalchemy.orm import (
    DeclarativeBase, Mapped, mapped_column, relationship, Session,
)

class Base(DeclarativeBase):
    pass

class Run(Base):
    __tablename__ = "runs"

    id: Mapped[int] = mapped_column(primary_key=True)
    energy_mev: Mapped[float]
    # связь "один ко многим": у захода — список измерений
    measurements: Mapped[list["Measurement"]] = relationship(back_populates="run")

class Measurement(Base):
    __tablename__ = "measurements"

    id: Mapped[int] = mapped_column(primary_key=True)
    run_id: Mapped[int] = mapped_column(ForeignKey("runs.id"))
    channel: Mapped[str]
    value: Mapped[float]
    run: Mapped[Run] = relationship(back_populates="measurements")

engine = create_engine("sqlite:///lab.db")  # та же база, поменяется только URL
Base.metadata.create_all(engine)            # создаёт таблицы по описанию классов

with Session(engine) as session:
    # объекты вместо INSERT: связи расставляются сами
    run = Run(energy_mev=12.5)
    run.measurements.append(Measurement(channel="BPM01", value=0.412))
    session.add(run)
    session.commit()

    # запрос вместо SELECT ... JOIN: строится из Python-выражений
    stmt = select(Measurement).join(Run).where(Run.energy_mev > 10)
    for m in session.execute(stmt).scalars():
        print(m.channel, m.value, m.run.energy_mev)
```

Заметь, что смена SQLite на PostgreSQL сводится к правке одной строки в `create_engine`, а написанный код не изменится.

ORM оправдан в приложении с десятком связанных таблиц и развивающейся схемой (накопленные миграции удобно вести инструментом [Alembic](https://alembic.sqlalchemy.org/)), при командной разработке и множестве типовых операций «создать-прочитать-обновить-удалить». Чистый SQL лучше для аналитических запросов с многоэтажными агрегатами и оконными функциями (SQL здесь короче и прозрачнее нагромождаемых ORM-конструкций), для массовых загрузок и разовых скриптов. И в любом случае ORM не освобождает от знания SQL. Под капотом он генерирует всё тот же SQL, и когда запрос внезапно тормозит, разбираться придётся со сгенерированным текстом (включи `create_engine(..., echo=True)` и посмотри, что реально уходит в базу).

## Нереляционные базы данных

Реляционная модель универсальна, но не единственна. Под конкретные профили нагрузки есть специализированные базы, называемые скопом «NoSQL». Знать их надо хотя бы обзорно, чтобы узнавать подходящую задачу в лицо.

Проще всех устроено хранилище пар «ключ → значение», [Redis](https://redis.io/). Оно живёт в оперативной памяти, и выполняемые операции занимают микросекунды. Типичными ролями становятся кеш (результат тяжёлого запроса или расчёта кладётся под ключ с заданным временем жизни), очереди задач между процессами (на Redis работают брокеры для Celery), счётчики и pub/sub-уведомления. Запись на диск есть, но включается по желанию. Redis отвечает за скорость, а не за главное хранилище истины.

Там, где у записей нет общей структуры, берут документные базы вроде [MongoDB](https://www.mongodb.com/). Единицей хранения служит документ, вложенная структура вроде JSON; документы собираются в коллекции, и жёсткой схемы нет, поэтому соседние документы могут иметь разные поля. Удобно для разнородных метаданных, меняющих структуру от записи к записи. Подвох в том, что «отсутствие схемы» означает лишь одно. Схема живёт не в базе, а в голове и в коде, и проверять её теперь тоже тебе.

Телеметрию установки, то есть давление в вакуумной камере, токи магнитов и температуры, приходящие с сотен датчиков каждую секунду годами, держат в базах временных рядов. Это то, что в ускорительной технике называется slow control, и [InfluxDB](https://www.influxdata.com/) заточена под такой поток «метка времени → значение» с тегами. Такие базы умеют автоматически прореживать и удалять устаревшие данные (retention policies), быстро агрегировать по окнам времени («среднее за каждую минуту последних суток») и штатно стыкуются с [Grafana](https://grafana.com/) для живых дашбордов мониторинга. Близким родственником в мире PostgreSQL выступает расширение TimescaleDB.

Наконец, для аналитики на миллиардах записей придуманы колоночные базы, самая известная из которых называется [ClickHouse](https://clickhouse.com/). Она хранит данные не по строкам, а по столбцам. Значения, принадлежащие одному столбцу, лежат рядом, отлично сжимаются, и запрос «среднее value по миллиарду строк» читает с диска только нужные столбцы, укладываясь в секунды на обычном сервере вместо часов и сохраняя привычный SQL-диалект. Платой становится слабость в точечных обновлениях и удалениях: колоночные базы рассчитаны на «дописывай и анализируй», а не на правку отдельных строк, разбросанных по таблице.

### Как выбирают: пример реальной системы

Обзор типов баз отвечает на вопрос «что бывает», но не на вопрос «что брать мне». Разберём это на живом примере — системе хранения данных ускорительного комплекса, использующей сразу три базы. Такой подход называется **полиглотным хранением** (polyglot persistence), при котором под каждый профиль данных берётся своё хранилище.

В PostgreSQL уходит всё, что должно быть строгим: пользователи и их роли, конфигурация ускорителя и его элементов, метаданные экспериментов (что за эксперимент, когда, кто ответственный), расписание работы установки и журнал событий. Общее у этих данных одно. Структура известна заранее, меняется редко, а целостность критична. Если запись эксперимента ссылается на несуществующего оператора, это ошибка, ловить которую должна база, а не код. Здесь и нужны схема, внешние ключи и ACID-транзакции.

В MongoDB складывают то, что разнородно: результаты измерений, данные пучка (траектории, размеры, интенсивность), временные ряды параметров, результаты моделирования и оптимизации. У складываемых данных нет и не может быть единой схемы: каждый новый тип эксперимента приносит свой набор полей. Требовать миграции базы под каждую новую методику — верный способ добиться того, чтобы данные складывали мимо базы, в файлы, которые копятся на рабочем столе. Гибкая схема здесь не уступка, она и есть требование задачи.

Redis держит недолго живущее: кеш частых запросов, текущие состояния устройств, сессии пользователей веб-интерфейса, очереди сообщений, идущих между компонентами. Ключевое слово здесь — «текущее». Значение тока магнита *прямо сейчас* спрашивают сотни раз в секунду, и ходить за ним в дисковую базу бессмысленно. Потеря этих данных при перезапуске неприятна, но не катастрофична: истина хранится в других двух базах.

Схему, определяющую выбор, можно свести к одному признаку на каждую базу.

| Признак данных | Подходящая база |
|---|---|
| Структура известна заранее, а нарушение связей недопустимо | PostgreSQL |
| Структура меняется от записи к записи | MongoDB |
| Данные нужны очень быстро, а их потеря не смертельна | Redis |

У полиглотного хранения есть и цена, о которой лучше знать заранее, до первой строчки кода. Как только данные разъезжаются по трём базам, **исчезает транзакция, охватывающая их все**, и одним атомарным действием уже не записать эксперимент в PostgreSQL вместе с его результатами в MongoDB. Согласованность приходится обеспечивать вручную, распределёнными транзакциями или компенсирующими действиями, откатывающими то, что успело записаться. Сложность, добавляемая этим, заметна. Окупается она, только когда профили данных действительно разные.

Отсюда вывод, полезный для студенческого проекта: **начинай с одной базы**. Одна PostgreSQL закрывает потребности лабораторного сервиса на годы вперёд, а для документов в ней есть тип `JSONB`, индексируемый по полям внутри документа. Вторую базу заводят не потому, что «так делают в больших системах», а когда первая измеримо перестала справляться с конкретным профилем нагрузки.

## Одна задача в четырёх хранилищах

Всё сказанное выше проще увидеть на примере, доводимом до кода. Возьмём учебный сервис, список записей с операциями «прочитать страницу», «добавить», «изменить», «удалить», и переложим его последовательно в четыре хранилища: текстовый файл, структуру в оперативной памяти, реляционную базу и кеш поверх неё.

Смотреть надо не на код, почти не меняющийся от варианта к варианту, а на то, где лежат данные и во что обходится каждая операция. Про сложность операций и структуры данных речь шла в главе [«Основные структуры данных»](../cs/basic-structures.md), и здесь мы увидим ту же арифметику на живом сервисе.

### Наивное решение. GET

Самым прямолинейным вариантом остаётся обычный текстовый файл, хранящий по строке на запись. Чтобы отдать одну страницу, приходится прочитать весь файл целиком, а при изменении и удалении ещё и переписать его заново, а это \\(O(n)\\) на каждую операцию.

```python
@app.route('/', methods=['GET'])
def paginated_get():
    page = int(request.args.get('page', '0'))
    first = page * PAGE_SIZE
    last = first + PAGE_SIZE
    result = []
    with open(FILE_PATH, 'r') as f:
        for i, line in enumerate(f.readlines()[first:last]):
            result.append( {'id': first + i, 'data': line.strip()})
    return {"result": result}
```

### Наивное решение. POST

```python
@app.route('/', methods=['POST'])
def post():
    data = request.json['data']
    with open(FILE_PATH, 'a') as f:
        f.write('\n' + data)
    return {}, 201
```

### Наивное решение. PUT

```python
@app.route('/<int:data_id>', methods=['PUT'])
def put(data_id):
    data = request.json['data']
    with open(FILE_PATH, 'r') as f:
        new_data = f.readlines()
        new_data[data_id] = data + '\n'
    with open(FILE_PATH, 'w') as f:
        f.writelines(new_data)
    return {}, 204
```

### Наивное решение. DELETE

```python
@app.route('/<int:data_id>', methods=['DELETE'])
def delete(data_id):
    with open(FILE_PATH, 'r') as f:
        new_data = f.readlines()
    del new_data[data_id]
    with open(FILE_PATH, 'w') as f:
        f.writelines(new_data)
    return {}, 204
```

### Фиксированный. GET

Если отвести каждой записи одинаковое число байт, файл превращается в массив, и нужную запись читаем сразу, перескочив к её смещению через `seek()`. Зато удаление дорожает: хвост файла, который тянется за ней, приходится сдвигать вручную.

```python
@app.route('/', methods=['GET'])
def paginated_get():
    page = int(request.args.get('page', '0'))
    first = page * PAGE_SIZE
    result = []
    with open(FILE_PATH, 'rb') as f:
        f.seek(first * ELEMENT_SIZE)
        data = f.read(PAGE_SIZE * ELEMENT_SIZE)
        for i, n in enumerate(range(PAGE_SIZE)):
            result.append(
                {
                    "id": first + i,
                    "data": (
                        data[n * ELEMENT_SIZE: (n + 1) * ELEMENT_SIZE].
                        strip(FILL_CHAR).decode('utf-8')
                    )
                }
            )
    return {"result": result}
```

### Фиксированный. POST

```python
def post():
    data = str(request.json['data'])
    with open(FILE_PATH, 'a+b') as f:
        f.write(data.encode('utf-8').ljust(ELEMENT_SIZE, FILL_CHAR))
    return {}, 201
```

### Фиксированный. PUT

```python
@app.route('/<int:data_id>', methods=['PUT'])
def put(data_id):
    data = request.json['data']
    with open(FILE_PATH, 'r+b') as f:
        f.seek(data_id * ELEMENT_SIZE)
        f.write(data.encode('utf-8').ljust(ELEMENT_SIZE, FILL_CHAR))
    return {}, 204
```

### Фиксированный. DELETE

```python
@app.route('/<int:data_id>', methods=['DELETE'])
def delete(data_id):
    with open(FILE_PATH, 'r+b') as f:
        point = data_id * ELEMENT_SIZE
        f.seek(point)
        while True:
            f.seek(point + ELEMENT_SIZE)
            complex_data = f.read(ELEMENT_SIZE)
            f.seek(point)
            if len(complex_data):
                f.write(complex_data)
                point += ELEMENT_SIZE
            else:
                f.truncate()
                break
    return {}, 204
```

### Database. GET

Настоящая база данных прячет всю эту механику за индексами, а искать по ключу, вставлять и удалять она умеет сама, и делает это не хуже структур, которые разбирались в этой главе.

```python
@app.route('/', methods=['GET'])
def paginated_get():
    page = int(request.args.get('page', '0'))
    with closing(psycopg2.connect(dbname=dbname, host=host)) as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                'SELECT "id", "todo" FROM "todos" ORDER BY "id" OFFSET %s LIMIT %s;',
                (page * PAGE_SIZE, PAGE_SIZE)
            )
            return {
                "result": [ {"id": row[0], "data": row[1]} for row in cursor]
            }
```

### Database. POST

```python
@app.route('/', methods=['POST'])
def post():
    data = str(request.json['data'])
    with closing(psycopg2.connect(dbname=dbname, host=host)) as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                'INSERT INTO "todos" ("todo") VALUES (%s) RETURNING "id";',
                (data,)
            )
            conn.commit()
            return {"id": cursor.fetchone()[0]}, 201
```

### Database. PUT

```python
@app.route('/<int:data_id>', methods=['PUT'])
def put(data_id):
    data = request.json['data']
    with closing(psycopg2.connect(dbname=dbname, host=host)) as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                'UPDATE "todos" SET "todo" = %s WHERE "id" = %s;',
                (data, data_id)
            )
            conn.commit()
            return {}, 204
```

### Database. DELETE

```python
@app.route('/<int:data_id>', methods=['DELETE'])
def delete(data_id):
    with closing(psycopg2.connect(dbname=dbname, host=host)) as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                'DELETE FROM "todos" WHERE "id" = %s;',
                (data_id,)
            )
            conn.commit()
            return {}, 204
```

### Cache. GET

Наконец, самые частые запросы можно вовсе не доводить до базы: готовый ответ кладётся в Redis и в следующий раз отдаётся прямо из оперативной памяти.

```python
@app.route('/', methods=['GET'])
def paginated_get():
    page = int(request.args.get('page', '0'))
                                
    redis_client = redis.StrictRedis(**redis_creds)
    cached_page = redis_client.get(page)
                                
    if cached_page:
        return cached_page
                                
    with closing(psycopg2.connect(**postgres_creds)) as conn:
        with conn.cursor() as cursor:
            cursor.execute(
                'SELECT "id", "todo" FROM "todos" ORDER BY "id" OFFSET %s LIMIT %s;',
                (page * PAGE_SIZE, PAGE_SIZE)
            )
            result = json.dumps({
                "result": [{"id": row[0], "data": row[1]} for row in cursor]
            })
            redis_client.set(page, result, ex=ttl)
            return result
```

## Pandas и базы данных

Мост между базой и анализом данных встроен в pandas. Функция `read_sql` выполняет запрос и возвращает `DataFrame`, а `to_sql` записывает `DataFrame` в таблицу.

```python
import pandas as pd
import sqlite3

con = sqlite3.connect("lab.db")

# фильтрация и агрегация выполняются в базе,
# в память попадает только готовая сводка
df = pd.read_sql(
    """
    SELECT r.energy_mev, AVG(m.value) AS mean_signal
    FROM measurements AS m
    JOIN runs AS r ON r.id = m.run_id
    WHERE m.channel = 'BPM01'
    GROUP BY r.id
    """,
    con,
)

# дальше — обычный pandas: график зависимости сигнала от энергии
df.plot.scatter(x="energy_mev", y="mean_signal")

# и обратно: результат анализа — в новую таблицу
df.to_sql("run_summary", con, if_exists="replace", index=False)
```

Тяжёлую фильтрацию и агрегацию отдавай базе (SQL с `WHERE` и `GROUP BY`), а в pandas загружай уже свёрнутый результат, тогда и память, и время расходуются на порядки экономнее, чем при подходе «прочитать всё и фильтровать в DataFrame». Связка «SQL-запрос → DataFrame → график» и даёт простейшее приложение для визуализации данных; про сами графики смотри главы про [обработку](python/numpy-and-pandas.md) и [визуализацию](python/visualization/practice.md) данных.

## С чего начать в своей лаборатории

Не нужно ждать большого проекта, чтобы применить всё это. Рецепт перехода от тысячи CSV к одной базе:

1. Заведи один файл `lab.db` и опиши схему. Минимально нужны таблицы вида `experiments`, `runs`, `measurements` с первичными и внешними ключами и `NOT NULL` на важных полях. Схема служит документацией, проверяющей сама себя.
2. Импортируй накопленные CSV одним проходящим по ним скриптом:

    ```python
    from pathlib import Path
    import pandas as pd
    import sqlite3

    con = sqlite3.connect("lab.db")

    for csv_path in Path("data").glob("**/*.csv"):
        df = pd.read_csv(csv_path)
        df["source_file"] = str(csv_path)  # не теряем происхождение данных
        df.to_sql("measurements_raw", con, if_exists="append", index=False)
    ```

    Дальше «сырую» таблицу можно разложить по нормализованной схеме уже SQL-запросами.
3. Новые данные пиши сразу в базу, применяя в скрипте сбора `executemany` пачками точек и `commit` раз в несколько секунд, а не файл на каждый заход.
4. Включи `PRAGMA journal_mode=WAL`: в режиме, включаемом так, SQLite позволит читать базу (строить графики) параллельно с идущей записью.
5. Смотреть данные глазами удобно в DB Browser for SQLite, а резервной копией служит обычная копия одного файла (`Connection.backup()` из Python делает её корректно даже во время работы).

Когда данными начнёт пользоваться вся группа с нескольких машин, перенесёшь схему в PostgreSQL; SQL и почти весь код при этом останутся прежними.

## Полезные ссылки

- [Документация SQLite](https://sqlite.org/docs.html)
- [SQLBolt, интерактивный учебник SQL с упражнениями](https://sqlbolt.com/)
- [Модуль sqlite3 в стандартной библиотеке Python](https://docs.python.org/3/library/sqlite3.html)
- [PostgreSQL Tutorial](https://www.postgresqltutorial.com/)
- [Документация SQLAlchemy](https://docs.sqlalchemy.org/)
- [DB Browser for SQLite](https://sqlitebrowser.org/)


> **Задание.** Собери и разверни всё вместе, приложение, контейнеры, сеть и базу: [«Деплой стартапа»](../practicum/kitty-startup-task.md).
